# Changelog

本プロジェクトの変更履歴（[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 準拠・[SemVer](https://semver.org/lang/ja/) 準拠）。

各バージョンの詳細な実行記録は `docs/local/manual_release-vX.Y.Z_YYYY-MM-DD.md`（非公開・gitignored）にある。ここは公開レポの正史。

## [Unreleased]

## [v0.7.0] - 2026-08-09

### Added
- **市区町村ごとの高校一覧ページを追加**（`/pref/<都道府県>/<市区町村>/`・1,265 ページ）。県ページは 1 県ぶんが一度に並ぶため、住んでいる市区町村だけを見たいときに探しづらかった。県ページの各市区町村セクションと、学校詳細の所在地欄から 1 タップで開ける。掲載は所在地の列挙で、通学できる範囲を示すものではない
- 県ページと市区町村ページに共通の内訳チップ（公立 / 私立 / 国立・課程・共学別学）。市区町村ページ側は新規で、県ページ側は実装を共通化した
- **地図に縮尺バーを追加**。バーの長さを固定してあるので、ズームしても画面上の長さが変わらず「この長さ = だいたい何 km」という距離感がそのまま身につく。通える範囲かどうかを地図の見た目だけで判断できる
- **学校詳細を「校名だけ見せる低いシート」で開けるようにした**。学校ページの「地図で見る」で着地したとき、従来は詳細が全画面で開いて地図が完全に隠れ、元のページに戻ったように見えていた。ハンドルか上スワイプで全画面へ展開できる
- 公開 API のフィールド定義を `DATA.md` に追加。各項目の型・収録条件・説明に加え、列挙値（学校種 / 設置者 / 共学別学 / 課程 / 校地種別 / 学校状態 / 募集状態）の意味と、`provenance`・`lifecycle`・`departments`・`admission_recruitment_units` の読み方を掲載。内容は `src/types/school.ts` と `scripts/lib/public-api.mjs` から生成し、実装とずれたらテストで落ちる
- 公開 API に中高一貫かどうか（`is_integrated`）を追加。`type`（高校 / 高専）とは別軸の属性で、これが無いと併設型・中等教育学校を利用者側で見分けられなかった
- 公開 API の更新をどう検知するかを `/data/` に記載
- 学校データ生成とアプリの直接取得に学校列の allowlist を導入し、収集作業用の内部メモを公開 JSON の生成経路から除外

### Changed
- **プリレンダーを React の実出力（SSR + hydration）に切り替え、初期表示のちらつきを根治**。従来は React が描くものとは別物の手書き HTML を `#root` に入れ、起動時にそれを丸ごと捨てて描き直していたため、スタイルの当たっていない素の HTML が一瞬見えていた。今はビルド時に描いた React の出力をブラウザ側がそのまま引き継ぐので、置き換えが起きない
- 公開 API の項目を実データに合わせて拡張し、出典方針の穴を塞いだ
- 一覧の設置者・課程・学校状態ラベルと桁区切りを言語設定に合わせ、SSR とブラウザの表示契約を統一

### Fixed
- 学校ページのパンくずリスト（構造化データ）で、市区町村の段に遷移先 URL が無く、検索結果のパンくず表示の対象外になっていたのを修正。市区町村ページの新設にあわせて実在するページを指すようにし、最後の段以外はすべて遷移先を持つことをビルド時に検査するようにした
- 埋め込んだ初期データを「正典」として扱わず、表示後に必ず最新を取り直すようにした。従来はデータだけ差し替えたときに `/data/`・法務・ガイド・学校詳細・県一覧で古い値が残ることがあった
- 学校名を検索地点にすると「群馬県立渋川工業高等学校」が「立渋川工業高等学校」と頭欠けして表示されていたのを修正。県名を省略する処理が、直後の「立」まで巻き込んでいた
- トップページの学校選びガイド節が英語表示に切り替わらなかったのを修正
- `DATA.md` の「収録内容」に入試統計・共学別学・中高一貫・校地種別が挙がっておらず、実際に公開している範囲より狭く書かれていたのを是正
- オフライン状態の SSR 初期値、検索 debounce の後始末、郵便番号の誤認、座標のない学校を検索索引へ混ぜる問題を修正
- 公開 API の自由記述に偏差値関連の語が混ざるケースと、学校の内部列が JSON / SSR 初期データへ混入するケースを静的検証で検出
- 公開 API の全学校レコードにある `is_integrated` が OpenAPI スキーマから抜けていたのを修正し、必須 boolean 項目として契約検査を追加
- GitHub Actions の SSR / SEO 生成にも build-time の公開 placeholder を明示し、CI のローカル環境依存を解消

### Security
- `schools` の匿名・認証済み直接 SELECT を公開列へ限定し、収集作業用の内部列を PostgREST 経由でも返さない migration を追加（本番適用はリリース runbook の DB ゲートで実施）

### Removed
- SSR 化で使われなくなった手書き HTML 生成を削除

## [v0.6.0] - 2026-08-07

### Added
- **学校基本情報を再利用できる公開 API（全件・都道府県別・メタデータ）と案内ページ `/data/` を追加**。偏差値の編集推計は含めず、学校公式 URL と項目単位の一次資料を同梱
- 項目単位の出典を管理する `school_field_sources` とフィールド定義 master を追加し、公開 API の生成時に一次資料の有無を検証
- フッターとサイドバーの「サービス情報」に「公開データとAPI」を追加し、地図画面を含む全画面から 1 ホップで `/data/` へ辿り着けるようにした（従来はプレスキット経由の 2 ホップのみ）
- `/data/` に県別エンドポイントを 47 件すべて列挙。従来は 1 件の例示だけで、残りの URL を利用者が推測する必要があった

### Changed
- 公式 URL と学校状態を一次資料で再検証し、南山高等学校を男子部・女子部へ分離。現在の収録校数を全国 47 都道府県・5,096 校へ更新
- 学校公式サイト URL を確認できた学校を 3,415 校から 5,088 校へ拡充（67.0% → 99.8%）。県教育委員会・市教育委員会・県私学協会・国立高専機構・文部科学省の通信制高校検索を出典として、公開 API から欠落する都道府県をゼロにした
- 公開 API、SEO、gzip、静的ファイル容量上限を build 時に一括検証するよう品質ゲートを強化
- フッターのリンク定義と公開方針の文言を `web/data/` の JSON へ一元化し、アプリ側の描画とプリレンダー HTML の食い違いをビルド時に検出するようにした

### Fixed
- 公開 API の出典ホスト許可リストに、私学協会など `ed/ac/lg/go.jp` を持たない一次資料の発行元が漏れていたのを補完。任意の `or.jp` / `gr.jp` を広く許可せず、収集時に内容を確認したホストだけを明示する
- サイドバーのプレスキット表記が日本語のハードコードで、英語表示でも日本語のまま出ていたのを修正

### Security
- 自宅地点の更新を認証利用者本人に限定する DB trigger と、更新回数を制限する ledger を追加

## [v0.5.0] - 2026-08-05

### Added
- **都道府県ハブ 47 枚（`/pref/<slug>/`）と全域ハブ（`/schools/`）を追加し、地図を開かずに全 5,095 校へ辿り着ける一覧導線を新設**。市区町村名は総務省 全国地方公共団体コードを単一の情報源として正規化
- 県ページに校名しぼりこみ、スクロールに追随する市区町村見出し、各校の参考偏差値と最新年度の第一次募集倍率の併記、学校ページから戻ったときの開閉状態・スクロール位置の復元
- トップの検索欄を統合検索へ拡張し、住所・郵便番号に加えて市区町村名・学校名の候補を提示。索引は検索欄にふれた時点で読み込み、トップで学校データ全件は読まない
- トップに地方別の開閉式リンクを追加（全域ハブ側は地方見出しごとの平坦なリンク一覧）
- 学校ページに近隣校リスト（直線距離を明記・既定は折りたたみ）、前身校と後継校の相互リンク、第一次募集の年度表、生徒数・男女比・中高一貫の規模ブロックを追加。年度表は公式出典 URL を確認できた学校だけに表示し、規模が未収集の学校は情報提供募集中を案内
- 学校ページから地図へ移る「地図で見る」導線（`/map?school=<id>`）。地図側は該当校へ寄せて詳細を開き、閉じると通常の地図 URL へ戻る
- 学校選びガイド 3 本（`/guide/<slug>`）: 通学時間と通学範囲の考え方 / 学校見学・文化祭のチェックリスト / 偏差値（編集推計）との付き合い方
- ボトムタブに「ホーム」を追加し、地図画面からトップの検索へ戻れるようにした
- `/press` の配布素材にプレスリリース PDF・ロゴ一式 zip・掲載可否チェックシート（印刷用 HTML）を追加
- データの二次利用方針をまとめた `DATA.md`（CC BY-SA 4.0・出典表記・偏差値を含めない理由）を追加し、README から参照
- 情報提供フォームの偏差値欄に、根拠として使える資料（学校公式の入試・進路資料、県教育委員会の公表資料、本人・保護者の同意がある得点通知）の案内を追加

### Changed
- 学校ページ（`/school/<id>/`）を、地図の上に詳細シートを重ねる構成から単独ページへ変更。地図ライブラリと学校データ全件を読まず、その学校だけの JSON を読む
- 学校詳細の選抜実績を年度と倍率の羅列から、年度 / 募集人員 / 志願者 / 受検者 / 合格者 / 倍率の表へ変更。受検者・合格者は年度内の全行が公表している場合のみ合算し、欠けがあれば出さない
- 存在しない URL は空のアプリ画面（HTTP 200）ではなく、専用の 404 ページを HTTP 404 で返すようにした（`noindex`・canonical なし）
- ログイン導線の文言を「LINEでログイン」「Google で続ける」に統一し、各社の公式配布アイコンを使うボタンへ変更
- トップの検索欄の案内を「住所・地名・学校名 どれでも 1 つで OK」へ改め、見つからないときは地方別リンクと現在地を案内する文面に変更。独立していた「学校名で探す」ボタンは統合検索と地方別リンクへ集約（学校名検索の画面自体は従来どおり利用できる）
- ヘッダーからボトムタブと重複するアイコン（トップの ≡ と ★、お気に入り・比較の 🏠）を削除
- ガイド・法務・`/press` ではボトムタブを出さないようにし、末尾スラッシュ付きの URL でも判定が効くよう正規化
- 配布資料 PDF（ポスター・配布資料）の収録範囲の表記を最新化して再出力
- README の今後の方針を「偏差値推計の全国展開」から「根拠を確認できた県・学校から掲載（全県の数値化は目標にしない）」へ改定
- `/press` の「初回公開」欄を「2026-07-05（群馬県版）。2026-07-31 に全国 47 都道府県へ拡大」へ更新（v0.4.0 の全国展開に表記を追随させる修正・v0.4.0 公開直後に本番反映済み）

### Fixed
- 過去の「公的資料を根拠とする独自推計」という偏差値の説明が、関東の一部値に未検証の編集推計を含む実態を十分に示していなかったため訂正。方法・掲載基準・過去の倍率由来値を 2026-07-12 に非表示化した経緯を「偏差値の方法と限界」ページ（`/legal/deviation-methodology`）で公開し、根拠を確認できない学校は数値を掲載しない方針を明確化。利用規約 第 4 条・プライバシーポリシー 第 5 節の同種表現も「公的資料を参考にした編集推計」へ改定
- 校名の短縮表示で接頭辞「国立」を設置者に関係なく落としていたため、東京都立国立高等学校が「高校」になるなど校名が壊れていた不具合を修正。国立の学校のときだけ落とすようにした
- 第一次募集の推移で、全日制が未公表の年度に定時制・通信制の倍率を代用していた不具合を修正。全日制のある学校では代表課程を固定し、未公表の年度は推移・平均から外す
- 前身校が現存する学校で、選抜実績の公式出典リンクが表示されなかった不具合を修正。学校データ生成時に出典の圧縮が二重にかかっていた
- 前身校の「公式根拠」リンクで、URL の検証結果が表示に反映されていなかった不具合を修正。検証できた URL のときだけリンクにし、あわせて関係の備考を表示
- トップの住所検索で、候補が 0 件のときや同じ語を入力し直したときに検索中の表示が消えず、そのまま操作できなくなる不具合を修正
- お気に入り・メモ・志望度の読み込みに失敗しても保存操作が通り、メモを空で上書きし得た問題を修正。1 つでも読み込みに失敗したら保存を止め、再読み込みを促す
- 志望度を続けて切り替えると、古い値が後から書き込まれることがあった不具合を修正。学校ごとに書き込み順序を直列化
- 家族グループの読み込みに失敗したときに「まだありません」と表示され、二重に作成できた不具合を修正。直前の状態を保持し、再試行を促す
- 重なったシートを Escape で閉じると、下のサイドバーやログインシートまで同時に閉じていた不具合を修正。最前面だけが反応する
- 中心地点の扱いを是正。別アカウントへ切り替えた後も前の利用者の地点が地図に残り、ブラウザに保存された値が壊れていても検証せずに地図へ渡していた
- 検索結果や共有リンクからそのタブで最初に開いた法務・`/press`・学校名検索の画面で、「←」がサイトの外へ出ていた不具合を修正。戻り先が無いときはトップへ移動する
- メンテナンス中は管理 API もメンテナンス画面に差し替わっていたため、管理者かどうかの判定が働かず管理画面の動線が出ていた不具合を修正。判定は権限エラーのときだけ切り替え、一時的な通信失敗では直前の状態を保つ（管理 API 自体のアクセス制限は従来どおり維持）
- 管理者の偏差値訂正で、暗証番号が違っても「訂正しました」と表示されていた不具合を修正
- 家族招待の受け取り待ち情報が端末に残り続け、別の利用者がその端末で拾い得た問題を修正。有効期限（10 分）とログイン利用者の紐付けを持たせ、成否にかかわらず処理後に破棄。匿名（ゲスト）のままでは受諾を試みず、ログイン導線を出す
- 学校ページの正規 URL（canonical・OGP）と sitemap を末尾スラッシュ付き（`/school/<id>/`）へ統一し、5,095 校すべてが検索エンジンにリダイレクト扱いされていた状態を解消。sitemap からは実体を持たない検索画面の URL を除外し、更新日をビルド日一律ではなく各校の実更新日へ変更（v0.4.0 公開直後に本番反映済み）
- 学校ページの初期 HTML で、閉校予定・募集停止の学校が現役校と同じ体裁になっていた不具合を修正。学校詳細と同じ学校状態・募集状態・前身校・旧名称を初期 HTML にも出力する（v0.4.0 公開直後に本番反映済み）
- ビルドごとに名前が変わる学校データ JSON が長期キャッシュの対象から外れ、再訪のたびに数 MB を取り直していた不具合を修正。`web/public/_headers` のパターンが gzip 化前のファイル名のままだった（v0.3.2 の gzip 化以降・v0.4.0 公開直後に本番反映済み）

### Security
- 家族共有のサーバー側権限判定を、認証情報を正典とする方式へ統一
- 家族への招待リンクに有効期限（発行から 7 日）を導入。期限切れのリンクは参加に使えない
- 利用状況の記録と情報提供フォームの受け口に、書き込める項目の限定と投稿レートの制限を追加・強化
- 管理者の暗証番号入力に、連続失敗時のロックを追加
- 学校の公式サイト URL は登録・更新時に形式を検証し、想定外の形式を保存できないようデータベース側で制約
- 管理 API のトークン検証経路を匿名キー経由に統一し、権限の強い鍵を検証経路から外した
- Content-Security-Policy 違反レポートの受け口を新設し、実際の配信内容を継続して把握できるようにした（適用は引き続き Report-Only のままで、表示挙動は変わらない）
- アクセス制御（RLS）を含むデータベース定義の基準スナップショットを公開レポジトリに追加し、権限設定を外部から検証可能にした

## [v0.4.0] - 2026-07-31

### Added
- **西日本 27 府県 2,522 校を追加し、対応地域を全国 47 都道府県・5,095 校へ拡大**（近畿・中国・四国・東海・九州・沖縄）。学校データは公的資料（文部科学省 学校基本調査・各府県教育委員会の公表資料等）からの転記のみ
- 入試選抜データを西日本各府県へ拡張（募集単位 6,214・選抜実績 26,969 行・出典 83,957 行）
- 佐賀 伊万里農林 / 伊万里商業を前身校として登録し、伊万里実業への統合関係（`merged_into`）を公式確認済みで紐付け
- `/press` 配布素材ページを拡充（配布素材カードにサムネイル画像・PDF ダウンロード動線）
- 学校詳細シートの学科 empty-state（学科データ未収集校で空欄のまま放置せず情報提供動線を提示）
- 校名下の読みがな（`name_kana`）表示。値が無い場合は「かな 情報提供募集中」で情報提供動線を紐付け
- 学科データが 1 件も無い学校を地図の学科フィルタで「その他」と区別できるようにした（`unknown` グループ）
- トップページに WebSite の構造化データを追加し、サイト名の別名として「まなびマップ」を宣言

### Changed
- サイト名の表記を `Manabi Map（まなびマップ）` に統一（ブラウザのタブ・SNS カード・画面ヘッダー・サイドバー）
- OGP の収録範囲（「全国 47 都道府県・5,095 校に対応。」）をビルド時に実データから生成するようにし、府県追加のたびに版ずれする構造を解消
- 地図画面のボトムシートを撤去し、地図の表示領域を拡大
- `PressPage` の版表記を動的化（`APP_VERSION` からリリース版数を自動取得）し、サイドバー動線ラベルを配布素材向けに拡張
- 学校詳細シートの空欄表示（学科・かな・公式サイト URL）を「情報提供募集中」の能動的な文言へ統一し、それぞれに情報提供動線を紐付け（「Manabi Map の完了判定原則」に基づく: Blocking 0 のデータ欠損は release blocker にせず、実利用の中で補完してもらう）

### Fixed
- `pnpm fetch:tags` を unshallow / deepen フォールバック方式へ変更し、Cloudflare Pages ビルド環境でも `APP_VERSION` が最新タグから正しく導出されるよう修正
- `school_departments.school_id` の索引欠落を修正。全国展開で学科が 7,798 行になった結果、学校データ生成クエリが Supabase のステートメントタイムアウトに達してビルドが失敗していた
- 入試履歴の取得を OFFSET ページングから keyset ページングへ変更。データ量の増加で深いページがタイムアウトしていた
- 学校ページのタイトルで都道府県名が二重に出ていた不具合を修正（例:「京都府京都府京田辺市」→「京都府京田辺市」）。222 校が該当し、うち 20 校は東京都の既存データ由来。市区町村の値を是正するとともに、表示側でも重複を落とすようにした

## [v0.3.3] - 2026-07-22

### Added
- メディア・教育関係者向けのプレスキットページ（`/press`）と、サイドバー・フッターからの導線
- 西日本展開に向けた学校候補の収集・PDF抽出・地理座標取得・候補検査ツール。西日本の学校データおよびv0.4用DB migrationの本番適用は、この版には含まれない

### Changed
- Dashboard snapshotが、外部APIの正常な空応答を失敗として扱わず、日次計測を継続できるよう改善

### Fixed
- 非管理者が管理画面URLを開いたとき、管理者判定後にトップへ戻るように修正。管理者APIのアクセス制限は維持

## [v0.3.2] - 2026-07-16

### Added
- 入試制度の原文を保った構造化モデル（募集単位・選抜段階・選抜区分・検査要素・指標別公式出典・品質フラグ）。倍率は保存せず募集人員と志願者数から算出し、比較可能な第一次募集だけを地図・時系列集計に使う
- 学校の法的ライフサイクル、募集状態、前身・後継関係、旧名称を表すmaster/FKテーブルと公式根拠・効力日。現行校詳細から前身校別の過年度入試を開いて確認できる
- 入試CSV v2の`school_record_key`対応。同名の旧校・新校を校名ではなく安定キーで区別して再生成できる

### Changed
- 東日本の学校再編44関係を法的学校単位へ分離し、再編前年度の入試実績65件を前身校へ付け替え。前身校値は現行校の現在倍率・時系列へ混ぜない
- 学校本体と構造化入試履歴を匿名APIからページ分割取得し、静的JSON生成時のPostgREST statement timeoutを回避
- 募集状態ラベルを「募集終了」「高校段階の外部募集なし」「募集開始前」「未確認」に分離

### Fixed
- 栃木県の今市工業・日光明峰・鹿沼南・鹿沼商工を、2026年度公式募集定員に基づき誤った募集停止状態から「募集中」へ訂正（統合予定は2027年度）

## [v0.3.1] - 2026-07-13

### Added
- 「情報募集中」項目からその場で情報提供・訂正報告できる簡易フォーム（`data_reports` テーブル + 管理者ダッシュボードのレポートタブで承認フロー）
- 管理者ダッシュボードの KPI タイルに前期間比 delta 表示（▲/▼/− + 数値 + 色。色だけに頼らずアクセシビリティ配慮）
- runtime メンテナンスモード切替（`app_config` テーブル + realtime 反映 + 管理者ダッシュボードのトグル UI + `scripts/maintenance.mjs` CLI）。従来の `VITE_MAINTENANCE_MODE` env var は DB 復元中など緊急時の保険として残す
- `school_deviation_values.estimate_basis` 列と `application_ratio_legacy AND is_active` を拒否する CHECK 制約（偏差値推計経路の恒久的品質防壁）
- 関東 6 都県の私立高校の入試実績 1,145 行（群馬 42/栃木 60/埼玉 100/千葉 177/東京 505/神奈川 261）。県教委・私学主管課の一括公表資料からの転記のみで、募集人員・応募者数が中心（受検/合格は一括資料に載らないため空欄・秋以降に追補予定）。茨城は一括公表なしにより対象外

### Changed
- 自宅設定後の地図初期ズームを、最寄り 15 校目までの距離ベース fitBounds に変更（10〜40km でクランプ・都市部は狭く / 地方は広く自動調整）。従来は東日本リージョン初期値の zoom 5（日本全図）に据え置かれ、毎回手動ズームが必要だった
- 管理者ダッシュボードの型を `web/src/types/admin.ts` に集約し、UI と Cloudflare Pages Functions で単一ソース化

### Fixed
- 保存済み自宅の復元経路でも通学圏へ自動 fit するよう修正（v0.3.0 東日本拡大でリージョン zoom が 5 に広がり顕在化していた症状）

## [v0.3.0] - 2026-07-12

### Added
- 東日本 13 道県（北陸 3・甲信越 3・東北 6・北海道）の学校データを投入し、対応地域を**東日本 20 都道県・2,589 校**へ拡大
- 入試実績（募集・志願・受検・合格 × 年度・20 都道県 × 直近 3 年）を学校詳細シートに表示（`school_admission_stats` テーブル新設・県教委等の公的資料の転記のみ）
- 管理者ダッシュボード基盤（日次スナップショット収集 workflow + Cloudflare Pages Functions の管理者 API + ダッシュボード画面）
- アプリ側メンテナンスモード（バナー表示 + 書込停止）
- schools.json の build hash 付き URL 化（`schools-manifest.json` 経由・キャッシュ反映ラグ解消）

### Changed
- ACTIVE_REGION を関東 1 都 6 県から東日本 20 都道県へ（郵便番号上 3 桁の複数レンジ対応・bbox 拡張・地図初期表示・検索例の文言）
- 校名の短縮表示を「立」を含まない公立正式名（北海道・宮城・長野の「北海道◯◯高等学校」形式）に対応
- secrets-scan にメールアドレス検知（allowlist 方式）を追加

### Fixed
- 2026-07-12 リリース前監査の修正一式（検索圏 bbox の福井嶺南・東京都島嶼の被覆漏れ / ホーム地点ラベルの県名処理一般化 / Supabase 直読みモードのページング / 長野県の読みがなデータ 58 件修正）

## [v0.2.4] - 2026-07-10

### Fixed
- OAuth callback の error クエリ検知と失敗のユーザー表示・LINE ボタン文言統一（Google ログイン有効化を含む）

## [v0.2.3] - 2026-07-09

### Added
- SEO 基盤（robots.txt / sitemap.xml / 学校別プリレンダー 1,362 校）・学校詳細シートの住所・郵便番号表示

## [v0.2.2] - 2026-07-08

### Added
- 偏差値修正ワークフロー（管理者上書き RPC・「私の記録」の [自署] ラベル・レビューキュー）・BottomTabBar・マイページ・リージョン内検索連動（地図 bounds 方式の絞り込み）

## [v0.2.1] - 2026-07-07

### Changed
- 偏差値バンドを「40 台 / 40 未満」に分割ほか UI 改善・version 表記の git タグ単一ソース化

## [v0.2.0] - 2026-07-07

### Added
- 関東 1 都 6 県への拡大（1,362 校）・学科分類の master 化（MEXT 17 分類 → UI 10 分類・`course_type_master` + trigger）

## [v0.1.5] - 2026-07-06

### Added
- Cloudflare Pages のレスポンスヘッダ設定 `web/public/_headers`（`X-Frame-Options: DENY` / `X-Content-Type-Options: nosniff` / `Referrer-Policy: strict-origin-when-cross-origin` / `Permissions-Policy` で camera/microphone/payment/usb を無効化）
- テスト基盤 vitest 導入（`web/vitest.config.ts` / `web/src/lib/format.test.ts` / `web/src/lib/geo.test.ts`）
- `pnpm-workspace.yaml` を追加し依存の hoisting を明示制御
- Supabase マイグレ `202607070201_v0.1.5_dedup_and_triggers.sql`（`user_school_deviations` の `department_id=NULL` 重複掃除 + `unique nulls not distinct` 化 + `moddatetime` トリガによる `updated_at` 自動更新）

### Changed
- `useUserData`: お気に入り/メモ/自己偏差値の取得失敗時に空データで上書きしない挙動へ改修。`toggleFavorite` を楽観更新 + DB 失敗時ロールバック方式へ変更
- `AppContext.persistHome`: fire-and-forget 設計を維持しつつ select/update/insert 失敗を `console.error` で追跡（住所値は PII のためログに載せない）
- `AuthContext`: `supabase.auth.getSession()` に catch を追加し、ストレージアダプタ異常でも `loading` がスタックしないよう修正

### Fixed
- （v0.1.4 派生 hotfix / v0.1.4 タグには含まれない・main には既に反映済）
  - nightly-backup workflow を URL 形式単一 Secret から `SUPABASE_DB_HOST/PORT/USER/NAME/PASSWORD` の split-secret + `PGPASSWORD` 方式へ書き換え。libpq URL parse エラー時の password fragment 漏洩を構造で排除
  - Supabase Free の Direct connection が IPv6-only で GHA runner から到達不能な問題を Session Pooler 経由に切替
  - Supabase 17.6 と Ubuntu 24.04 標準の `pg_dump` v16.14 の meta-version 不一致を PostgreSQL 公式 apt repo + `postgresql-client-17` で解消

## [v0.1.4] - 2026-07-06

### Added
- nightly-backup workflow（`.github/workflows/nightly-backup.yml`）: 毎日 JST 03:00 に Supabase を `pg_dump` → gzip → age 暗号化 → Cloudflare R2 `nightly/YYYY-MM-DD.dump.gz.age` へ PUT。30 日保持
- AdSlot 骨組み（`src/data/ad-slots.ts` / `src/lib/utm.ts` / `AdSlot.tsx`）と UTM 自動付与（`utm_source=manabi-map` / `utm_medium=<placement>` / `utm_campaign=v0.1.4-launch` / `utm_content=school-<id>`）。ダミー案件のみ・実案件は ASP 承認後の別 diff
- 法務ページに「5.5 広告表示について」節（AdSense 不使用・PR ラベル明示・UTM は集計目的）と利用規約「第 9 条 広告」を詳細化

### Changed
- Leaflet attribution UX 改修: `map.attributionControl.setPrefix(false)` で「Leaflet |」を消去。学校詳細シート open 時は `body[data-sheet-open="true"]` トグルで attribution を CSS 非表示
- attribution テキストロジックを `src/lib/attribution.ts` に集約（Protomaps 化に向けた下地）

## [v0.1.3] - 2026-07-06

### Added
- 課程（全日制 / 定時制 / 通信制）と連携校スキーマ（`school_course_time[]` / `main_school_name` / `campus_type`）を追加
- 全校生徒数 + 年度（`total_students` / `enrollment_year`）と男女比（`male_ratio`）フィールド
- 群馬県立太田フレックス高等学校を追加（群馬 79 校に）
- 学校詳細シートに「規模: 約 800 人（2026 年）」「男女比: 男 55% / 女 45%」表示
- schools 静的化: `web/scripts/gen-schools-json.mjs` で Supabase から `web/public/schools.json` を吐き出し、`useSchools` はデフォルトで `/schools.json` を fetch（Supabase egress を Free 枠 5 GB 内に）
- float-bar / bottom sheet に「課程」フィルタ 3 チップ（**通信制は初期 OFF**）

### Changed
- Cloudflare Web Analytics（クッキーレス）有効化

## [v0.1.2.3] - 2026-07-05

### Added
- OGP メタタグと hero 画像を追加（SNS シェア対応）

## [v0.1.2.2] - 2026-07-05

### Fixed
- 徒歩/自転車の所要時間係数を現実寄りに調整（徒歩 12 → 15 min/km・自転車 4 → 5 min/km）

## [v0.1.2.1] - 2026-07-05

### Fixed
- 桐生市立商業高校が「商業高校」に短縮されてどの市か分からなくなる短縮校名バグを修正

## [v0.1.2] - 2026-07-05

### Added
- 密集ピンを `leaflet.markercluster` でクラスタ化
- 4 モードの所要時間（徒歩 / 自転車 / バス / 車）表示
- float-bar フィルタを 4 分類ドロップダウン + 半径スライダーに刷新
- ピンラベルを 2 行化して長い校名でも偏差値を見せる
- 偏差値フィルタに「未測定」オプション追加
- 学科フィルタに「総合学科」を独立グループとして追加
- PC でマウスホバー時にピンを最前面 + アクセント枠

### Fixed
- 学科フィルタが `commercial_*` / `agricultur*` の細分値を拾えていなかった問題
- float-bar の `overflow:auto` でポップオーバーがクリップされる問題

### Changed
- CLAUDE.md に広告ポリシーを Non-negotiable として追記

## [v0.1.1] - 2026-07-05

### Added
- 校舎アイコン（ピン内部モチーフを本から校舎へ）
- UI 仕上げとドキュメント刷新
- ライセンス確定（コード AGPL-3.0-or-later / データ CC BY-SA 4.0）

## [v0.1.0] - 2026-07-05

### Added
- 群馬県版 MVP を https://manabi-map.app で本番公開
- 住所ジオコーディング（OSM Nominatim / 400ms デバウンス）
- Leaflet + OpenStreetMap タイルでの地図表示
- お気に入り機能・学校ごとの家族メモ
- LINE ログイン（Custom OIDC・非 OIDC 構成で HS256 問題を回避）+ 匿名ログイン
- Cloudflare Pages Git 連携による main push = 本番自動デプロイ
