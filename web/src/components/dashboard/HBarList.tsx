interface HBarListProps { rows: { label: string; value: number }[] }
export function HBarList({ rows }: HBarListProps) { const max = Math.max(1, ...rows.map((row) => row.value)); return <div>{rows.map((row) => <div key={row.label} style={{ display: 'grid', gridTemplateColumns: 'minmax(90px,180px) 1fr 64px', gap: 8, alignItems: 'center', margin: '6px 0' }}><span title={row.label} style={{ overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{row.label}</span><span style={{ height: 14, background: '#e8622a', width: `${Math.max(2, row.value / max * 100)}%` }} /><b style={{ textAlign: 'right', fontVariantNumeric: 'tabular-nums' }}>{row.value.toLocaleString()}</b></div>)}</div> }
