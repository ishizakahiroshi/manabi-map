# Supabase 接続情報（実値は web/.env.local に置く・.env.local は gitignored）
# 値は Supabase Dashboard → Project Settings → Data API から取得する
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key
